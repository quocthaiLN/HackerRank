#include "function.h"

int main()
{
	list l1;
	//list l2;
	inputList(l1);
	//inputList(l2);
	//inputList(l2);
	//printListReverse(l1.pHead);
	//printList(l2);
	//addHead(l1, 0);
	//sortList(l1);
	//list l2 = removeDuplicates(l1);
	//deleteNode(l1, 2);
	//cout << compareLists(l1, l2);
	//cout << getNodeValue(l1, 4);
	//reverseList(l1);
	//printList(l1);
	//sortList(l1);
	//sortList(l2);
	//printList(l1);
	//printList(l2);
	//list l3 = mergeList(l1, l2);
	//printList(l3);
	cout << hasCycle(l1);
	return 0;
}